<p id="notice"><%= notice %></p>
<% if user_signed_in? %>
 <% if @attending.present? %>
    <%= link_to "Unjoin event", attending_path(@attending), method: :delete %>
    <% else %>
    <%= link_to "Join Event", event_attending_index_path(@event), method: :post %>
  <% end %>
  <% end %>
<p>
  <strong>Title:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Start time:</strong>
  <%= @event.start_time %>
</p>

<p>
  <strong>End time:</strong>
  <%= @event.end_time %>
</p>

<p>
  <strong>Location:</strong>
  <%= @event.location %>
</p>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
